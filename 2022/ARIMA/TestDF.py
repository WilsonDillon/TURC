import pandas as pd

# resultsDF = pd.DataFrame(columns = ['Actual', 'Predicted'])
# print(resultsDF)
new_row = pd.DataFrame([[3, 2.5]], columns=['Actual', 'Predicted'])
print(new_row)
# resultsDF = pd.concat([resultsDF, new_row])

# print(resultsDF)